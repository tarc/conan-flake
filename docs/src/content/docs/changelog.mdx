---
title: Changelog
editUrl: https://codeberg.org/tarcisio/conan-flake/_edit/main/CHANGELOG.md
---

{/*
  The revision history is maintained in `CHANGELOG.md` at the repository root,
  and this chapter presents that file rather than a second copy of it: the
  import below renders the real file, so the chapter cannot drift from it.

  The path resolves the same way in the checkout and in the build sandbox — the
  Astro project root is `docs/`, and the build copies the pinned `CHANGELOG.md`
  next to it.

  site.GUIDES.9
*/}

import { Content as Changelog } from "../../../../CHANGELOG.md";

<Changelog />
