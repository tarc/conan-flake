# devenv
.devenv
devenv.local.nix
/devenv.lock
/devenv.yaml

# CMake build
CMakeUserPresets.json
build*/

# mdBook output: `mdbook build`/`just docs-serve` render the site here, next to
# its sources; the site that gets published is built by Nix instead.
/docs/book/
/result

# git-hooks
.pre-commit-config.yaml

# woodpecker metadata
tarcisio-conan-flake-pipeline-*-metadata.json

# claude
**/.claude/settings.local.json
.claude/settings.json
.claude/agents
.mcp.json
.devcontainer.json

# local dotenv
.env

# opencode
opencode.jsonc
.opencode/

# task planning (local only)
/tmp/
/.tasks/
