# devenv
.devenv
devenv.local.nix
/devenv.lock
/devenv.yaml

# CMake build
CMakeUserPresets.json
build*/

# Astro output and working directories: `astro build`/`just docs-serve` render
# the site and cache their intermediate state here, next to its sources; the
# site that gets published is built by Nix instead. `node_modules` is a copy of
# a Nix store path, placed by the development shell.
/docs/node_modules
/docs/dist/
/docs/.astro/
/result

# git-hooks
.pre-commit-config.yaml

# woodpecker metadata
tarcisio-conan-flake-pipeline-*-metadata.json

# claude
**/.claude/settings.local.json
.claude/settings.json
.claude/agents
.mcp.json
.devcontainer.json

# local dotenv
.env

# opencode
opencode.jsonc
.opencode/

# task planning (local only)
/tmp/
/.tasks/
